"""
Admin configuration for matching app
"""
# pyrefly: ignore [missing-import]
from django.contrib import admin

# The matching app doesn't have its own models
# It uses UserProfile from the users app
# No custom admin configuration needed
